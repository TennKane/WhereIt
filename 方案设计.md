# 🏠 WhereIt — 家庭物品收纳管理小程序 · 方案设计

> 核心痛点：**"东西放哪了？"** → 打开小程序搜索即得完整路径。

---

## 一、项目定位

一个极简的家庭物品位置管理工具，让用户以 **"房间 → 家具 → 层/格"** 的三级结构组织物品，通过快速搜索直达存放位置。

---

## 二、数据库设计 (Drizzle ORM + SQLite)

### 2.1 表结构

```
rooms
├── id          TEXT PK
├── name        TEXT NOT NULL         -- "客厅" / "厨房"
├── sort_order  INT  DEFAULT 0        -- 排序
├── created_at  TEXT DEFAULT NOW
└── updated_at  TEXT DEFAULT NOW

furnitures
├── id          TEXT PK
├── room_id     TEXT FK → rooms.id
├── name        TEXT NOT NULL         -- "茶几" / "衣柜"
├── layers      INT  DEFAULT 1        -- 层数 (1=无分层)
├── description TEXT                  -- 备注 (尺寸/颜色等)
├── sort_order  INT  DEFAULT 0
├── created_at  TEXT
└── updated_at  TEXT

items
├── id              TEXT PK
├── furniture_id    TEXT FK → furnitures.id
├── layer_index     INT  DEFAULT 0    -- -1=桌面, 0=第1层, 1=第2层...
├── name            TEXT NOT NULL     -- "电视遥控器"
├── description     TEXT              -- 备注
├── quantity        INT  DEFAULT 1
├── image           TEXT              -- 图片路径 (可选)
├── created_at      TEXT
└── updated_at      TEXT
```

### 2.2 数据库关系

```
Room 1──N Furniture 1──N Item
```

### 2.3 索引

- `idx_items_name` ON `items(name)` — 全文搜索用
- `idx_furnitures_room` ON `furnitures(room_id)` — 按房间查家具
- `idx_items_furniture` ON `items(furniture_id)` — 按家具查物品

---

## 三、路由设计 (Next.js App Router)

```
/                              → 首页：概览仪表盘
/rooms                         → 房间列表
/rooms/new                     → 新建房间
/rooms/[roomId]                → 房间详情 → 家具列表
/rooms/[roomId]/furniture/new  → 添加家具（含层数设置）
/rooms/[roomId]/furniture/[furnitureId]  
                               → 家具详情 → 分层展示物品
/items/new?furnitureId=xxx     → 添加物品（选择层号）
/items/[itemId]/edit           → 编辑物品
/search                        → 快速搜索
  ?q=遥控器                     → 结果项显示完整路径
```

### 布局嵌套

```
RootLayout
└── Navbar + Sidebar (全局搜索入口)
    ├── page.tsx           (/ 仪表盘)
    ├── rooms/
    │   ├── page.tsx       (房间列表)
    │   ├── new/page.tsx   (新建房间)
    │   └── [roomId]/
    │       ├── page.tsx   (房间内家具)
    │       ├── furniture/
    │       │   ├── new/page.tsx
    │       │   └── [furnitureId]/page.tsx  (分层物品)
    │       └── layout.tsx (面包屑导航上下文)
    ├── items/
    │   ├── new/page.tsx
    │   └── [itemId]/edit/page.tsx
    └── search/page.tsx
```

---

## 四、UI / 组件设计

### 4.1 页面设计

| 页面 | 布局 | 关键组件 |
|------|------|----------|
| **仪表盘** | 统计卡片 + 最近存放 + 快捷搜索 | `StatCard`, `RecentItems`, `QuickSearch` |
| **房间列表** | 网格卡片 | `RoomCard` (+ 家具数量角标) |
| **房间详情** | 家具网格 / 列表 | `FurnitureCard` (+ 物品数量 + 层数标识) |
| **家具详情** | 分层 Tab / 垂直分段 | `LayerTabs`, `ItemCard` |
| **添加物品** | 表单弹窗 / 页面 | `ItemForm` (react-hook-form + zod) |
| **搜索结果** | 结果列表 + 路径面包屑 | `SearchResultItem` (完整路径) |

### 4.2 核心交互组件

**LayerTabs** — 家具分层切换
```
┌─────────────────────────────────┐
│  [桌面]  [第1层]  [第2层]  [第3层] │
├─────────────────────────────────┤
│  · 电视遥控器                     │
│  · 抽纸                          │
└─────────────────────────────────┘
```

**SearchResultItem** — 搜索结果项
```
┌─────────────────────────────────────┐
│  🔍 电视遥控器                        │
│  📍 客厅 → 茶几 → 第2层               │
│  📝 黑色，两个                        │
└─────────────────────────────────────┘
```

**BreadcrumbNav** — 面包屑导航
```
🏠 首页 > 客厅 > 茶几
```

### 4.3 视觉风格

- 干净的卡片式布局，柔和阴影
- 每个房间/家具用不同的柔和色块区分（使用 tailwind 调色板）
- 搜索框常驻顶部，全局可访问
- 右侧抽屉式侧栏显示物品详情

---

## 五、数据流

### 5.1 搜索流程（核心场景）

```
用户输入 "遥控器"
  → /search 页 (Server Component)
  → DB: SELECT items WHERE name LIKE '%遥控器%'
  → JOIN furnitures, rooms 组装完整路径
  → 渲染 SearchResultItem 列表
```

### 5.2 CRUD 流程

```
页面操作 → React Server Action / API Route
  → Zod schema 校验
  → Drizzle ORM 操作 SQLite
  → revalidatePath() / redirect()
  → UI 自动更新
```

---

## 六、技术实现要点

### 6.1 搜索方案

- **基础版**：SQL `LIKE %keyword%` 查询，对千级数据量足够
- **进阶**：可引入 SQLite FTS5 全文索引，实现分词搜索和模糊匹配
- 搜索结果 JOIN rooms + furnitures 表，拼出完整路径

### 6.2 物品图片

- 初期：不上传图片，纯文本描述
- 可选：集成上传组件，图片存本地 `public/uploads/` 或对象存储

### 6.3 分层模型

- `furnitures.layers` 字段存储总层数
- `items.layer_index`：-1 表示"桌面/顶部"，0 开始表示各层
- 家具详情页根据 `layers` 动态生成 Tab

### 6.4 表单校验

```typescript
// room schema
const roomSchema = z.object({
  name: z.string().min(1, "房间名不能为空").max(20),
})

// furniture schema
const furnitureSchema = z.object({
  name: z.string().min(1, "家具名不能为空").max(30),
  layers: z.coerce.number().int().min(1).max(20).default(1),
})

// item schema
const itemSchema = z.object({
  name: z.string().min(1, "物品名不能为空").max(50),
  furnitureId: z.string().uuid(),
  layerIndex: z.coerce.number().int().default(0),
  quantity: z.coerce.number().int().min(1).default(1),
  description: z.string().max(200).optional(),
})
```

### 6.5 认证 (可选)

- 初期可跳过，单机使用
- 如需多用户/云端，集成 next-auth v5

---

## 七、目录结构

```
src/
├── app/                    # App Router 页面
│   ├── page.tsx
│   ├── layout.tsx
│   ├── rooms/
│   ├── items/
│   └── search/
├── components/
│   ├── ui/                 # shadcn 基础组件
│   ├── rooms/              # RoomCard, RoomForm...
│   ├── furnitures/         # FurnitureCard, LayerTabs...
│   ├── items/              # ItemCard, ItemForm, SearchResultItem...
│   └── shared/             # Breadcrumb, SearchBar, EmptyState...
├── lib/
│   ├── db/
│   │   ├── schema.ts       # Drizzle 表定义
│   │   ├── index.ts        # DB 连接
│   │   └── migrations/     # 迁移文件
│   └── actions/            # Server Actions
│       ├── room.ts
│       ├── furniture.ts
│       └── item.ts
├── validations/            # Zod schemas
│   ├── room.ts
│   ├── furniture.ts
│   └── item.ts
└── types/                  # 共用类型
    └── index.ts
```

---

## 八、实现阶段建议

| 阶段 | 内容 | 产出 |
|------|------|------|
| **Phase 1** 基建 | Next.js 初始化 + Drizzle schema + SQLite + shadcn 配置 | 可跑通的基础项目 |
| **Phase 2** 核心 CRUD | 房间 / 家具 / 物品 的增删改查 | 完整的管理闭环 |
| **Phase 3** 分层与搜索 | 分层 Tab 交互 + 全局搜索 + 路径展示 | 核心价值功能 |
| **Phase 4** 体验打磨 | 空状态 / Toast / 确认弹窗 / 响应式 / 动效 | 可用性提升 |
| **Phase 5** 可选增强 | 认证 / 图片上传 / 导出备份 / 数据统计 | 锦上添花 |

---

## 九、关键设计决策

1. **为什么不用树形组件？** 树形组件在移动端交互笨重，三级结构用页面跳转 + 面包屑更直观
2. **分层用 Tab 而非手风琴？** Tab 切换成本低，一目了然当前在第几层
3. **Server Action 还是 API Route？** 优先 Server Action，减少客户端-服务端往返，配合 `revalidatePath` 即时刷新
4. **本地优先**：SQLite 零配置，数据文件即备份，用户无部署成本
