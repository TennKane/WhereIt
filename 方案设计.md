# 🏠 WhereIt — 家庭物品收纳管理工具 · 方案设计

> 核心痛点：**"东西放哪了？"** → 打开应用搜索即得完整路径。

---

## 一、项目定位

一个极简的家庭物品位置管理工具，让用户以 **"场所 → 区域 → 储物 → 层/格"** 的四级结构组织物品，通过快速搜索直达存放位置。
支持多场所（家、公司、租房）隔离管理，邮箱注册登录保护数据安全。

---

## 二、数据库设计 (Drizzle ORM + SQLite/Turso)

### 2.1 表结构

```
users
├── id              TEXT PK
├── email           TEXT NOT NULL UNIQUE
├── password_hash   TEXT NOT NULL         -- bcrypt 哈希
└── created_at      TEXT DEFAULT NOW

locations
├── id              TEXT PK
├── name            TEXT NOT NULL         -- "家" / "公司" / "租房"
├── sort_order      INT  DEFAULT 0
├── created_at      TEXT DEFAULT NOW
└── updated_at      TEXT DEFAULT NOW

zones
├── id              TEXT PK
├── location_id     TEXT FK → locations.id
├── name            TEXT NOT NULL         -- "客厅" / "卧室" / "工位"
├── sort_order      INT  DEFAULT 0
├── created_at      TEXT DEFAULT NOW
└── updated_at      TEXT DEFAULT NOW

storages
├── id              TEXT PK
├── zone_id         TEXT FK → zones.id
├── name            TEXT NOT NULL         -- "茶几" / "衣柜" / "书架"
├── layers          INT  DEFAULT 1        -- 层数 (1=无分层)
├── description     TEXT                  -- 备注（尺寸/颜色等）
├── sort_order      INT  DEFAULT 0
├── created_at      TEXT DEFAULT NOW
└── updated_at      TEXT DEFAULT NOW

items
├── id              TEXT PK
├── storage_id      TEXT FK → storages.id
├── layer_index     INT  DEFAULT 0        -- -1=桌面, 0=第1层, 1=第2层...
├── name            TEXT NOT NULL         -- "电视遥控器"
├── description     TEXT                  -- 备注
├── quantity        INT  DEFAULT 1
├── image           TEXT                  -- 图片路径 (可选)
├── created_at      TEXT DEFAULT NOW
└── updated_at      TEXT DEFAULT NOW
```

### 2.2 数据库关系

```
Location 1──N Zone 1──N Storage 1──N Item
```

### 2.3 索引

- `idx_items_name` ON `items(name)` — 全文搜索用
- `idx_zones_location` ON `zones(location_id)` — 按场所查区域
- `idx_storages_zone` ON `storages(zone_id)` — 按区域查储物
- `idx_items_storage` ON `items(storage_id)` — 按储物查物品

---

## 三、路由设计 (Next.js App Router)

```
/                              → 首页：概览仪表盘（需登录）
/login                         → 邮箱登录
/register                      → 邮箱注册
/locations                     → 场所列表
/locations/new                 → 新建场所
/locations/[locationId]        → 场所详情 → 区域列表
/locations/[locationId]/zones/new          → 添加区域
/locations/[locationId]/zones/[zoneId]     → 区域详情 → 储物列表
/locations/[locationId]/zones/[zoneId]/storages/new        → 添加储物（含层数）
/locations/[locationId]/zones/[zoneId]/storages/[storageId] → 储物详情 → 分层物品
/items/new?storageId=xxx       → 添加物品（选择层号）
/items/[itemId]/edit           → 编辑物品
/search?q=关键词                → 快速搜索（完整路径展示）
```

### 路由守卫

所有页面（除 `/login`、`/register`）由 `proxy.ts` 中间件保护，未登录跳转到 `/login`。  
Session 使用 JWT 存储在 httpOnly cookie 中，7 天有效期。

---

## 四、UI / 组件设计

### 4.1 页面设计

| 页面 | 布局 | 关键组件 |
|------|------|----------|
| **仪表盘** | 统计卡片 + 最近存放 + 快捷入口 | `StatCard`, `RecentItems` |
| **场所列表** | 网格卡片 | `LocationCard` (+ 区域/储物数量角标) |
| **场所详情** | 区域网格 | `ZoneCard` (+ 储物数量) |
| **区域详情** | 储物网格 | `StorageCard` (+ 物品数量 + 层数标识) |
| **储物详情** | 分层展示物品 | `ItemCard` (分层分组) |
| **新建/编辑** | 表单页 | 各 `*Form` 组件 (useActionState + Toast) |

### 4.2 核心交互

**分层展示** — 储物详情页按层分组展示物品
```
[第 1 层]     → 电视遥控器 x1, 抽纸 x2
[第 2 层]     → 充电器 x3
[桌面]        → 台灯 x1
```

**搜索结果** — 完整路径展示
```
🔍 电视遥控器
📍 家 → 客厅 → 茶几 → 第 2 层
📝 黑色，两个
```

### 4.3 视觉风格

- 干净卡片式布局，柔和阴影
- 每个层级用不同图标区分（MapPin → Layers → Box → Package）
- 搜索框常驻顶部导航栏，全局可访问
- 明暗主题切换（next-themes）

---

## 五、数据流

### 5.1 搜索流程（核心场景）

```
用户输入 "遥控器"
  → Navbar 搜索框（400ms 防抖）
  → /search 页 (Server Component)
  → DB: SELECT items WHERE name LIKE '%遥控器%'
  → JOIN storages, zones, locations 组装完整路径
  → 渲染 SearchResultItem 列表
```

### 5.2 CRUD 流程

```
页面操作 → React Server Action (useActionState)
  → Zod schema 校验 → 失败返回 fieldErrors
  → Drizzle ORM 操作 SQLite/Turso
  → 成功返回 → client 端显示 Toast 提示 → router.push 跳转
  → revalidatePath() 刷新服务端缓存
```

### 5.3 防连点机制

所有表单提交按钮在请求进行中自动 `disabled`（依赖 `useActionState` 的 `isPending`），防止用户重复点击导致重复创建。

---

## 六、认证方案

### 6.1 登录注册

- 邮箱 + 密码，bcryptjs 哈希存储
- 注册时检查邮箱唯一性
- 登录成功签发 JWT（HS256，7天有效）
- Token 存储在 httpOnly、sameSite cookie 中

### 6.2 中间件守卫

`src/proxy.ts` 拦截所有请求，检查 cookie 中的 JWT：
- 无效/过期 → 重定向到 `/login`
- `/login`、`/register` 路径放行

### 6.3 退出

`logout` Server Action 清除 cookie，重定向到登录页

---

## 七、技术实现要点

### 7.1 搜索方案

- SQL `LIKE %keyword%` 查询，对千级数据量足够
- 搜索结果 JOIN storages + zones + locations 表，拼出完整路径（4 级路径）
- 支持从 Navbar 回车搜索或 400ms 自动跳转

### 7.2 物品图片

- 当前：纯文本描述，不上传图片
- 未来：集成上传组件，图片存 `public/uploads/` 或对象存储

### 7.3 分层模型（当前）

- `storages.layers` 字段存储总层数
- `items.layer_index`：-1 表示"桌面/顶部"，0 开始表示各层
- 储物详情页根据 `layers` 动态生成层分组

### 7.4 规格模型（待实现）

将储物的"层数"升级为 **"层 × 格"** 二维规格：

**数据库变更：**
```
storages 表移除 layers 字段，新增：
├── shelves   TEXT NOT NULL   -- JSON: [{ name: "第一层", rows: [{ name: "第一格" }, { name: "第二格" }] }, ...]

items 移除 layer_index，新增：
├── shelf_index   INT DEFAULT 0    -- 第几层
├── row_index     INT DEFAULT 0    -- 第几格
```

**创建储物流程（弹窗内完成）：**
1. 输入储物名称、备注
2. 添加层，可命名层（默认"第一层""第二层"...）
3. 为每层设置格数，可命名格（默认"第一格""第二格"...）
4. 保存后 shelves JSON 写入 storages 表

**储物详情页展示（不跳页）：**
```
第一层
├── 第一格 → [物品A， 物品B]
├── 第二格 → [物品C]
第二层
├── 第一格 → [物品D]
```

**物品定位：** `shelf_index` + `row_index` 确定物品在储物中的精确位置，不再只是简单的"第几层"。

**数据迁移：** 现有 `storages.layers` 数据迁移为 `shelves` JSON 格式，`items.layer_index` 迁移为 `shelf_index` + `row_index=0`。

### 7.4 表单校验

```typescript
// location schema
const locationSchema = z.object({
  name: z.string().min(1, "场所名不能为空").max(20),
})

// zone schema
const zoneSchema = z.object({
  name: z.string().min(1, "区域名不能为空").max(20),
  locationId: z.string().min(1),
})

// storage schema
const storageSchema = z.object({
  name: z.string().min(1, "储物名不能为空").max(30),
  zoneId: z.string().min(1),
  layers: z.coerce.number().int().min(1).max(20).default(1),
  description: z.string().max(200).optional(),
})

// item schema
const itemSchema = z.object({
  name: z.string().min(1, "物品名不能为空").max(50),
  storageId: z.string().min(1),
  layerIndex: z.coerce.number().int().default(0),
  quantity: z.coerce.number().int().min(1).default(1),
  description: z.string().max(200).optional(),
})
```

---

## 八、目录结构

```
src/
├── app/                    # App Router 页面
│   ├── page.tsx            # 仪表盘
│   ├── layout.tsx          # 根布局（Navbar + ThemeProvider + Toaster）
│   ├── login/              # 登录
│   ├── register/           # 注册
│   ├── locations/          # 场所 CRUD
│   │   ├── page.tsx
│   │   ├── new/page.tsx
│   │   └── [locationId]/
│   │       ├── page.tsx
│   │       └── zones/
│   │           ├── new/page.tsx
│   │           └── [zoneId]/
│   │               ├── page.tsx
│   │               └── storages/
│   │                   ├── new/page.tsx
│   │                   └── [storageId]/page.tsx
│   ├── items/
│   │   ├── new/page.tsx
│   │   └── [itemId]/edit/page.tsx
│   └── search/page.tsx
├── components/
│   ├── forms/              # 客户端表单组件（useActionState + Toast）
│   │   ├── location-create-form.tsx
│   │   ├── zone-create-form.tsx
│   │   ├── storage-create-form.tsx
│   │   ├── item-create-form.tsx
│   │   ├── item-edit-form.tsx
│   │   ├── login-form.tsx
│   │   └── register-form.tsx
│   ├── ui/                 # 基础 UI 组件
│   └── shared/             # Navbar, ThemeProvider, EmptyState
├── lib/
│   ├── db/
│   │   ├── schema.ts       # Drizzle 表定义
│   │   ├── index.ts        # DB 连接
│   │   └── migrations/     # 迁移文件
│   └── actions/            # Server Actions
│       ├── auth.ts         # 登录、注册、退出
│       ├── location.ts
│       ├── zone.ts
│       ├── storage.ts
│       └── item.ts
└── proxy.ts                # JWT 验证守卫中间件
```

---

## 九、实现阶段

| 阶段 | 内容 | 状态 |
|------|------|------|
| **Phase 1** 基建 | Next.js + Drizzle schema + SQLite + Tailwind 配置 | ✅ |
| **Phase 2** 核心 CRUD | 场所 / 区域 / 储物 / 物品 的增删改查 | ✅ |
| **Phase 3** 分层与搜索 | 分层展示 + 全局搜索 + 4 级路径 | ✅ |
| **Phase 4** 体验打磨 | 空状态 / Toast / 防连点 / 响应式 / 明暗主题 | ✅ |
| **Phase 5** 认证 | 邮箱注册登录 + JWT Session + 路由守卫 | ✅ |
| **Phase 6** 可选增强 | 图片上传 / 导出备份 / 图表统计 | ⏳ |

---

## 十、关键设计决策

1. **为什么四级结构（场所→区域→储物→物品）？** 覆盖家庭 + 工作地 + 租房等多场景，一次部署管理多场所
2. **分层用分组而非 Tab？** 储物层数通常较少（1-3 层），垂直分组比 Tab 切换更直观，信息一览无余
3. **Server Action 而非 API Route？** 减少客户端-服务端往返，配合 `revalidatePath` 即时刷新，`useActionState` 天然支持 pending 状态
4. **原生 form 而非 react-hook-form？** 表单简单（2-4 字段），Server Action + `useActionState` 即够用，无需额外状态库
5. **JWT 而非 next-auth？** 自部署场景 JWT + bcrypt 依赖最少，无外部服务耦合
6. **本地优先**：SQLite 零配置，Turso 兼容协议，数据文件即备份
